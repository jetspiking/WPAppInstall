<Window x:Class="WPAppInstall.DialogPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPAppInstall"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="DialogTitle"  Width="350">  
    <Grid HorizontalAlignment="Center" Width="auto">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Hidden" Width="auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel  Orientation="Vertical">
                <Image Name="DialogImage" Width="200"/>
                <TextBlock Name="DialogText" TextWrapping="Wrap"/>
                <Image Name="DeviceImage" Width="250" Margin="0,5,0,0"/>
            </StackPanel>
        </ScrollViewer>
        <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Bottom" Height="25" HorizontalAlignment="Center" Margin="10,10,10,10" Name="DialogButtons"/>
    </Grid>
</Window>
